#include "main.h"

/**
*mul - a function gives last digit form SRAND
*@a: first additive
*@b: second additive
*Return: 1 if c is lowercase and 0 otherwise
*/

int mul(int a, int b)
{
	return (a * b);
}
