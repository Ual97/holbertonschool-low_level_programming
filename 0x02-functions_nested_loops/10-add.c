#include "main.h"

/**
*add - a function gives last digit form SRAND
*@x: first additive
*@y: second additive
*Return: 1 if c is lowercase and 0 otherwise
*/

int add(int x, int y)
{
	return (x + y);
}
